# Scripts Directory

Utility and automation scripts for the project.

## Planned Scripts (Phase 2+)

```
scripts/
├── setup_env.sh          # One-command environment setup
├── download_models.py    # Download pre-trained weights
├── prepare_dataset.py    # Dataset preparation pipeline
├── run_inference.py      # CLI tool for running inference on a video file
├── export_onnx.py        # Export PyTorch models to ONNX
└── migrate_db.py         # Database migration helper
```
