/**
 * Custom React hooks for the AI Driver Safety Platform.
 *
 * Phase 2+ will add:
 *   - useSession()       — fetch & subscribe to an analysis session
 *   - useAuth()          — Supabase Auth state management
 *   - useRealtime()      — Supabase Realtime subscription for processing updates
 *   - useVideoUpload()   — chunked video upload with progress tracking
 */

export {};
